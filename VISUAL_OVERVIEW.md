# 📊 Visual System Overview

## 🏗️ System Architecture Diagram

```
┌──────────────────────────────────────────────────────────────────────────┐
│                         CLIENT (Browser/Mobile App)                       │
└────────────────────────────────┬─────────────────────────────────────────┘
                                 │ HTTP/JSON
                                 ▼
┌──────────────────────────────────────────────────────────────────────────┐
│                    🌐 PRESENTATION LAYER (Controller)                     │
│  ┌────────────────────────────────────────────────────────────────────┐  │
│  │ UserController.java                                                 │  │
│  │ ─────────────────────────────────────────────────────────────────  │  │
│  │ • POST   /api/users              (Create BASIC user)               │  │
│  │ • POST   /api/users/premium      (Create PREMIUM user)             │  │
│  │ • GET    /api/users/{id}         (Get by ID)                       │  │
│  │ • GET    /api/users/email/{email} (Get by email)                   │  │
│  │ • GET    /api/users              (Get all - paginated)             │  │
│  │ • GET    /api/users/active       (Get active users)                │  │
│  │ • GET    /api/users/type/{type}  (Get by account type)             │  │
│  │ • GET    /api/users/search       (Search users)                    │  │
│  │ • GET    /api/users/recent       (Get recent users)                │  │
│  │ • PATCH  /api/users/{id}         (Update user)                     │  │
│  │ • DELETE /api/users/{id}         (Soft delete)                     │  │
│  │ • POST   /api/users/{id}/upgrade (Upgrade to PREMIUM)              │  │
│  │                                                                     │  │
│  │ Responsibilities:                                                   │  │
│  │ ✓ HTTP request/response handling                                   │  │
│  │ ✓ Input validation (@Valid)                                        │  │
│  │ ✓ Response formatting (ApiResponse wrapper)                        │  │
│  │ ✓ OpenAPI/Swagger documentation                                    │  │
│  └────────────────────────────────────────────────────────────────────┘  │
└────────────────────────────────┬─────────────────────────────────────────┘
                                 │
                                 ▼
┌──────────────────────────────────────────────────────────────────────────┐
│                    ⚙️  SERVICE LAYER (Business Logic)                     │
│  ┌────────────────────────────────────────────────────────────────────┐  │
│  │ UserService.java                                                    │  │
│  │ ─────────────────────────────────────────────────────────────────  │  │
│  │ • createUser(request)          → UserResponse                      │  │
│  │ • createPremiumUser(request)   → UserResponse                      │  │
│  │ • getUserById(id)              → UserResponse                      │  │
│  │ • getUserByEmail(email)        → UserResponse                      │  │
│  │ • getAllUsers(pageable)        → Page<UserResponse>                │  │
│  │ • getActiveUsers()             → List<UserResponse>                │  │
│  │ • getUsersByType(type, page)   → Page<UserResponse>                │  │
│  │ • searchUsers(query, type)     → Page<UserResponse>                │  │
│  │ • getRecentUsers(days)         → List<UserResponse>                │  │
│  │ • updateUser(id, request)      → UserResponse                      │  │
│  │ • deleteUser(id)               → void                              │  │
│  │ • upgradeUserToPremium(id)     → UserResponse                      │  │
│  │                                                                     │  │
│  │ Responsibilities:                                                   │  │
│  │ ✓ Business logic orchestration                                     │  │
│  │ ✓ Transaction management (@Transactional)                          │  │
│  │ ✓ Coordinates: Factory → Strategy → Repository → Mapper           │  │
│  │ ✓ Business rule enforcement (email uniqueness, etc.)               │  │
│  └────────────────────────────────────────────────────────────────────┘  │
└──────────┬──────────────┬──────────────┬──────────────┬─────────────────┘
           │              │              │              │
           ▼              ▼              ▼              ▼
┌──────────────┐  ┌──────────────┐  ┌──────────┐  ┌──────────────┐
│   FACTORY    │  │  STRATEGY    │  │REPOSITORY│  │   MAPPER     │
│   PATTERN    │  │   PATTERN    │  │ PATTERN  │  │  PATTERN     │
└──────────────┘  └──────────────┘  └──────────┘  └──────────────┘
       │                  │              │              │
       ▼                  ▼              ▼              ▼
┌──────────────────────────────────────────────────────────────────────────┐
│                    🎨 PATTERN IMPLEMENTATIONS                             │
│                                                                           │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │ 1️⃣  FACTORY PATTERN (Object Creation)                            │   │
│  │ ───────────────────────────────────────────────────────────────  │   │
│  │ UserFactory.java (interface)                                     │   │
│  │ UserFactoryImpl.java (implementation)                            │   │
│  │                                                                   │   │
│  │ What it does:                                                     │   │
│  │ • Validates email format (regex)                                 │   │
│  │ • Validates password strength (min 8 chars, complexity)          │   │
│  │ • Hashes password with BCrypt (work factor 10)                   │   │
│  │ • Sets default values (isActive=true, emailVerified=false)       │   │
│  │ • Creates BASIC vs PREMIUM users with different rules            │   │
│  │                                                                   │   │
│  │ Methods:                                                          │   │
│  │ • createUser(CreateUserRequest) → User (BASIC)                   │   │
│  │ • createPremiumUser(CreateUserRequest) → User (PREMIUM)          │   │
│  │ • updateUser(User, UpdateUserRequest) → User                     │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                           │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │ 2️⃣  STRATEGY PATTERN (Validation Rules)                          │   │
│  │ ───────────────────────────────────────────────────────────────  │   │
│  │ ProfileValidationStrategy.java (interface)                       │   │
│  │ BasicProfileValidationStrategy.java                              │   │
│  │ PremiumProfileValidationStrategy.java                            │   │
│  │ ValidationStrategyFactory.java (context)                         │   │
│  │                                                                   │   │
│  │ BASIC Validation (BasicProfileValidationStrategy):               │   │
│  │ ✓ Email required (valid format)                                  │   │
│  │ ✓ Password required (min 8 chars)                                │   │
│  │ ✓ Name required (2-255 chars)                                    │   │
│  │                                                                   │   │
│  │ PREMIUM Validation (PremiumProfileValidationStrategy):           │   │
│  │ ✓ All BASIC requirements                                         │   │
│  │ ✓ Headline required (min 10 chars, max 255)                      │   │
│  │ ✓ Summary required (min 50 chars, max 2000)                      │   │
│  │ ✓ Location required (min 3 chars, max 255)                       │   │
│  │                                                                   │   │
│  │ ValidationStrategyFactory:                                        │   │
│  │ • getStrategy(AccountType) → ProfileValidationStrategy           │   │
│  │ • Uses EnumMap for efficient lookup                              │   │
│  │ • Validates completeness at startup (all types have strategy)    │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                           │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │ 3️⃣  REPOSITORY PATTERN (Data Access)                             │   │
│  │ ───────────────────────────────────────────────────────────────  │   │
│  │ UserRepository.java (interface extends JpaRepository)            │   │
│  │                                                                   │   │
│  │ Built-in methods (from JpaRepository):                           │   │
│  │ • save(user)                                                      │   │
│  │ • findById(id)                                                    │   │
│  │ • findAll()                                                       │   │
│  │ • findAll(pageable)                                               │   │
│  │ • delete(user)                                                    │   │
│  │ • count()                                                         │   │
│  │                                                                   │   │
│  │ Custom query methods (10 total):                                 │   │
│  │ • findByEmail(email) → Optional<User>                            │   │
│  │ • existsByEmail(email) → boolean                                 │   │
│  │ • findByIsActive(isActive) → List<User>                          │   │
│  │ • findByAccountType(type, page) → Page<User>                     │   │
│  │ • countByAccountType(type) → long                                │   │
│  │ • findByIsActiveAndAccountType(active, type) → List<User>        │   │
│  │ • findByCreatedAtAfter(date) → List<User>                        │   │
│  │ • searchUsers(query, page) → Page<User>                          │   │
│  │ • searchUsersByAccountType(query, type, page) → Page<User>       │   │
│  │ • findUsersCreatedInLastNDays(days) → List<User>                 │   │
│  └──────────────────────────────────────────────────────────────────┘   │
│                                                                           │
│  ┌──────────────────────────────────────────────────────────────────┐   │
│  │ 4️⃣  DTO PATTERN (Data Transfer)                                  │   │
│  │ ───────────────────────────────────────────────────────────────  │   │
│  │ UserMapper.java (MapStruct interface)                            │   │
│  │                                                                   │   │
│  │ Methods:                                                          │   │
│  │ • toResponse(User) → UserResponse                                │   │
│  │ • toResponseList(List<User>) → List<UserResponse>                │   │
│  │ • updateFromDto(UpdateUserRequest, User) → void                  │   │
│  │ • toEntity(CreateUserRequest) → User                             │   │
│  │                                                                   │   │
│  │ DTOs:                                                             │   │
│  │ • CreateUserRequest: email, password, name, headline, etc.       │   │
│  │ • UpdateUserRequest: All fields optional (partial update)        │   │
│  │ • UserResponse: Safe fields only (no password, no audit fields)  │   │
│  └──────────────────────────────────────────────────────────────────┘   │
└───────────────────────────────────────────────────────────────────────────┘
                                 │
                                 ▼
┌──────────────────────────────────────────────────────────────────────────┐
│                    🗄️  DATA LAYER (PostgreSQL)                           │
│  ┌────────────────────────────────────────────────────────────────────┐  │
│  │ users TABLE                                                         │  │
│  │ ─────────────────────────────────────────────────────────────────  │  │
│  │ Columns (20):                                                       │  │
│  │ • id (BIGSERIAL PRIMARY KEY)                                        │  │
│  │ • email (VARCHAR(255) UNIQUE)                                       │  │
│  │ • password (VARCHAR(255)) ← BCrypt hash                            │  │
│  │ • name (VARCHAR(255))                                               │  │
│  │ • headline (VARCHAR(255))                                           │  │
│  │ • summary (TEXT)                                                    │  │
│  │ • location (VARCHAR(255))                                           │  │
│  │ • profile_photo_url (VARCHAR(512))                                  │  │
│  │ • account_type (VARCHAR(20) DEFAULT 'BASIC')                        │  │
│  │ • is_active (BOOLEAN DEFAULT true)                                  │  │
│  │ • email_verified (BOOLEAN DEFAULT false)                            │  │
│  │ • phone_number (VARCHAR(20))                                        │  │
│  │ • date_of_birth (DATE)                                              │  │
│  │ • industry (VARCHAR(100))                                           │  │
│  │ • current_job_title (VARCHAR(255))                                  │  │
│  │ • current_company (VARCHAR(255))                                    │  │
│  │ • created_at (TIMESTAMP)                                            │  │
│  │ • updated_at (TIMESTAMP)                                            │  │
│  │ • created_by (VARCHAR(100))                                         │  │
│  │ • updated_by (VARCHAR(100))                                         │  │
│  │                                                                     │  │
│  │ Indexes (8):                                                        │  │
│  │ • idx_users_email (email)                                           │  │
│  │ • idx_users_account_type (account_type)                             │  │
│  │ • idx_users_created_at (created_at DESC)                            │  │
│  │ • idx_users_active_account_type (is_active, account_type)           │  │
│  │ • idx_users_name_lower (LOWER(name))                                │  │
│  │ • idx_users_headline_lower (LOWER(headline))                        │  │
│  │ • idx_users_location_lower (LOWER(location))                        │  │
│  │ • idx_users_email_verified (email_verified)                         │  │
│  │                                                                     │  │
│  │ Constraints:                                                        │  │
│  │ • PRIMARY KEY (id)                                                  │  │
│  │ • UNIQUE (email)                                                    │  │
│  │ • CHECK (account_type IN ('BASIC', 'PREMIUM'))                      │  │
│  └────────────────────────────────────────────────────────────────────┘  │
└──────────────────────────────────────────────────────────────────────────┘
```

---

## 🔄 Request Flow Example: Create Premium User

```
1. HTTP Request
   │
   │ POST /api/users/premium
   │ Content-Type: application/json
   │ {
   │   "email": "jane@example.com",
   │   "password": "SecurePass456",
   │   "name": "Jane Smith",
   │   "headline": "Senior Software Engineer",
   │   "summary": "10 years experience...",
   │   "location": "San Francisco, CA"
   │ }
   │
   ▼
2. UserController.createPremiumUser()
   │
   ├─ Bean Validation runs (@Valid)
   │  ✓ @NotNull, @Email, @Size, etc.
   │
   ├─ Delegates to service layer
   │  userService.createPremiumUser(request)
   │
   ▼
3. UserService.createPremiumUser()
   │
   ├─ STEP 1: Check email uniqueness
   │  userRepository.existsByEmail(email)
   │  ↓ SQL: SELECT COUNT(*) FROM users WHERE email = ?
   │  ↓ If exists → throw ValidationException
   │
   ├─ STEP 2: Create user entity (Factory Pattern)
   │  userFactory.createPremiumUser(request)
   │  │
   │  ├─ Validate email format (regex)
   │  ├─ Validate password strength
   │  ├─ Validate premium requirements (headline, summary, location)
   │  ├─ Hash password with BCrypt
   │  ├─ Set account type to PREMIUM
   │  ├─ Set defaults (isActive=true, emailVerified=false)
   │  └─ Return User entity
   │
   ├─ STEP 3: Validate profile (Strategy Pattern)
   │  validationStrategyFactory.getStrategy(AccountType.PREMIUM)
   │  ↓ Returns PremiumProfileValidationStrategy
   │  │
   │  strategy.validate(user)
   │  ├─ Validate email (not null, valid format)
   │  ├─ Validate password (not null, min 8 chars)
   │  ├─ Validate name (not null, 2-255 chars)
   │  ├─ Validate headline (not null, min 10 chars)
   │  ├─ Validate summary (not null, min 50 chars)
   │  └─ Validate location (not null, min 3 chars)
   │
   ├─ STEP 4: Save to database (Repository Pattern)
   │  userRepository.save(user)
   │  ↓ Hibernate generates SQL
   │  ↓ INSERT INTO users (...) VALUES (...)
   │  ↓ Returns saved user with generated ID
   │
   ├─ STEP 5: Convert to DTO (DTO Pattern)
   │  userMapper.toResponse(savedUser)
   │  ↓ MapStruct auto-mapping
   │  ↓ Excludes password and audit fields
   │  └─ Returns UserResponse
   │
   └─ Return UserResponse
   │
   ▼
4. UserController wraps in ApiResponse
   │
   ApiResponse.success(response, "Premium user created successfully")
   │
   ▼
5. HTTP Response (201 Created)
   │
   {
     "success": true,
     "message": "Premium user created successfully",
     "data": {
       "id": 42,
       "email": "jane@example.com",
       "name": "Jane Smith",
       "headline": "Senior Software Engineer",
       "summary": "10 years experience...",
       "location": "San Francisco, CA",
       "accountType": "PREMIUM",
       "isActive": true,
       "emailVerified": false,
       "createdAt": "2025-12-20T19:30:00"
     },
     "timestamp": "2025-12-20T19:30:00"
   }
```

---

## 🔐 Security Flow

```
┌─────────────────────────────────────────────────────────────────┐
│ Password Hashing (BCrypt)                                        │
│ ─────────────────────────────────────────────────────────────── │
│                                                                  │
│ User Input:                                                      │
│   "SecurePass123"                                                │
│                                                                  │
│         ▼                                                        │
│                                                                  │
│ BCryptPasswordEncoder.encode()                                   │
│   • Generates random salt                                        │
│   • Applies BCrypt algorithm (work factor 10)                    │
│   • Produces 60-character hash                                   │
│                                                                  │
│         ▼                                                        │
│                                                                  │
│ Stored in Database:                                              │
│   "$2a$10$N9qo8uLOickgx2ZMRZoMye8rVMyK9T..."                     │
│                                                                  │
│   Format: $2a$10$[22-char salt][31-char hash]                    │
│            │  │   │             │                                │
│            │  │   │             └─ 31-char hash value            │
│            │  │   └─ 22-char salt (unique per password)          │
│            │  └─ Cost factor (2^10 = 1024 iterations)            │
│            └─ BCrypt version                                     │
│                                                                  │
│ ✅ Benefits:                                                     │
│   • Unique salt per password (prevents rainbow table attacks)   │
│   • Slow hashing (prevents brute force)                          │
│   • Industry standard                                            │
└─────────────────────────────────────────────────────────────────┘

┌─────────────────────────────────────────────────────────────────┐
│ DTO Protection (Preventing Sensitive Data Exposure)             │
│ ─────────────────────────────────────────────────────────────── │
│                                                                  │
│ ❌ Without DTO (Exposing Entity):                               │
│                                                                  │
│   GET /api/users/1                                               │
│   ↓                                                              │
│   {                                                              │
│     "id": 1,                                                     │
│     "email": "john@example.com",                                 │
│     "password": "$2a$10$...",  // 🚨 BCrypt hash exposed!       │
│     "createdBy": "admin",      // 🚨 Internal detail            │
│     "updatedBy": "system"      // 🚨 Internal detail            │
│   }                                                              │
│                                                                  │
│ ✅ With DTO (Safe Response):                                    │
│                                                                  │
│   GET /api/users/1                                               │
│   ↓                                                              │
│   {                                                              │
│     "id": 1,                                                     │
│     "email": "john@example.com",                                 │
│     "name": "John Doe",                                          │
│     "accountType": "BASIC",                                      │
│     "createdAt": "2025-12-20T19:30:00"                           │
│   }                                                              │
│   // ✅ No password, no audit fields                            │
└─────────────────────────────────────────────────────────────────┘
```

---

## 📊 Technology Stack Layers

```
┌──────────────────────────────────────────────────────────────────┐
│ FRONT-END (Future)                                                │
│ ─────────────────────────────────────────────────────────────    │
│ React / Angular / Vue.js                                          │
│ HTTP REST calls to backend                                        │
└──────────────────────────────────────────────────────────────────┘
                             │
                             ▼
┌──────────────────────────────────────────────────────────────────┐
│ API GATEWAY (Future)                                              │
│ ─────────────────────────────────────────────────────────────    │
│ Spring Cloud Gateway / Kong / Envoy                               │
│ • Routing                                                         │
│ • Rate limiting                                                   │
│ • Authentication                                                  │
└──────────────────────────────────────────────────────────────────┘
                             │
                             ▼
┌──────────────────────────────────────────────────────────────────┐
│ MICROSERVICES                                                     │
│ ─────────────────────────────────────────────────────────────    │
│ ┌──────────────────────┐  ┌────────────────────────────────┐    │
│ │ USER SERVICE ✅      │  │ POSTS SERVICE (Future)         │    │
│ │ • User management    │  │ • Create/edit posts            │    │
│ │ • Profile CRUD       │  │ • Like/comment                 │    │
│ │ • Account types      │  │ • News feed                    │    │
│ └──────────────────────┘  └────────────────────────────────┘    │
│                                                                   │
│ ┌──────────────────────┐  ┌────────────────────────────────┐    │
│ │ AUTH SERVICE (Next)  │  │ CONNECTIONS SERVICE (Future)   │    │
│ │ • Login/logout       │  │ • Friend requests              │    │
│ │ • JWT tokens         │  │ • Network graph                │    │
│ │ • Refresh tokens     │  │ • Recommendations              │    │
│ └──────────────────────┘  └────────────────────────────────┘    │
└──────────────────────────────────────────────────────────────────┘
                             │
                             ▼
┌──────────────────────────────────────────────────────────────────┐
│ FRAMEWORKS & LIBRARIES                                            │
│ ─────────────────────────────────────────────────────────────    │
│ Spring Boot 3.2.1        (Application framework)                 │
│ Spring Data JPA          (Data access)                            │
│ Spring Security          (Authentication)                         │
│ Spring MVC               (REST controllers)                       │
│ Hibernate                (ORM)                                    │
│ MapStruct 1.5.5          (Entity-DTO mapping)                     │
│ Lombok                   (Boilerplate reduction)                  │
│ JJWT 0.12.3              (JWT tokens)                             │
│ SpringDoc OpenAPI 2.3.0  (API documentation)                      │
│ Bean Validation          (Input validation)                       │
└──────────────────────────────────────────────────────────────────┘
                             │
                             ▼
┌──────────────────────────────────────────────────────────────────┐
│ DATABASES & CACHING                                               │
│ ─────────────────────────────────────────────────────────────    │
│ PostgreSQL 15+  (Primary database)                               │
│ Redis 7+        (Caching, Pub/Sub) [Future]                      │
│ Flyway          (Database migrations)                             │
│ HikariCP        (Connection pooling)                              │
└──────────────────────────────────────────────────────────────────┘
                             │
                             ▼
┌──────────────────────────────────────────────────────────────────┐
│ MESSAGING & EVENTS                                                │
│ ─────────────────────────────────────────────────────────────    │
│ Kafka 3.6+      (Event streaming) [Future]                       │
│ • UserCreatedEvent                                                │
│ • UserUpdatedEvent                                                │
│ • ProfileUpgradedEvent                                            │
└──────────────────────────────────────────────────────────────────┘
                             │
                             ▼
┌──────────────────────────────────────────────────────────────────┐
│ INFRASTRUCTURE                                                    │
│ ─────────────────────────────────────────────────────────────    │
│ Docker          (Containerization) [Future]                      │
│ Kubernetes      (Orchestration) [Future]                         │
│ Helm            (K8s package manager) [Future]                   │
└──────────────────────────────────────────────────────────────────┘
                             │
                             ▼
┌──────────────────────────────────────────────────────────────────┐
│ OBSERVABILITY                                                     │
│ ─────────────────────────────────────────────────────────────    │
│ Prometheus      (Metrics) [Future]                               │
│ Grafana         (Dashboards) [Future]                            │
│ ELK Stack       (Logging) [Future]                               │
│ Zipkin          (Distributed tracing) [Future]                   │
│ Spring Actuator (Health checks)                                  │
└──────────────────────────────────────────────────────────────────┘
```

---

## 📁 Module Structure

```
linkedin-system/
│
├── 📦 common-lib (Shared library)
│   ├── 📄 ApiResponse.java          (Success wrapper)
│   ├── 📄 ErrorResponse.java        (Error wrapper)
│   ├── 🚨 BaseException.java        (Abstract base)
│   ├── 🚨 ValidationException.java  (400 errors)
│   ├── 🚨 ResourceNotFoundException (404 errors)
│   ├── 🚨 UnauthorizedException     (403 errors)
│   └── 🚨 BusinessException         (422 errors)
│
└── 📦 user-service (User microservice)
    ├── 🌐 UserController            (12 REST endpoints)
    ├── ⚙️  UserService               (Business logic)
    ├── 🗄️  UserRepository            (Data access - 10 methods)
    ├── 👤 User                       (JPA entity)
    ├── 🎯 AccountType                (BASIC, PREMIUM enum)
    ├── 📝 BaseAuditEntity            (created_at, updated_at, etc.)
    │
    ├── 📦 DTOs
    │   ├── UserResponse              (API response)
    │   ├── CreateUserRequest         (Creation DTO)
    │   └── UpdateUserRequest         (Update DTO)
    │
    ├── 🎨 Design Patterns
    │   ├── 🏭 UserFactory            (Factory Pattern)
    │   │   ├── UserFactory.java      (Interface)
    │   │   └── UserFactoryImpl.java  (Implementation)
    │   │
    │   ├── 🎯 Validation Strategy    (Strategy Pattern)
    │   │   ├── ProfileValidationStrategy.java (Interface)
    │   │   ├── BasicProfileValidationStrategy.java
    │   │   ├── PremiumProfileValidationStrategy.java
    │   │   └── ValidationStrategyFactory.java (Context)
    │   │
    │   ├── 🗄️  Repository Pattern
    │   │   └── UserRepository.java   (Spring Data JPA)
    │   │
    │   └── 📦 DTO Pattern
    │       └── UserMapper.java       (MapStruct)
    │
    ├── ⚙️  Configuration
    │   ├── AuditingConfig            (JPA auditing)
    │   └── PasswordEncoderConfig     (BCrypt)
    │
    ├── 🚨 Exception Handling
    │   └── GlobalExceptionHandler    (Centralized)
    │
    └── 📊 Database
        └── V1__Create_users_table.sql (Flyway migration)
```

---

## 🎯 Design Pattern Relationships

```
┌─────────────────────────────────────────────────────────────────┐
│                      USER SERVICE                                │
│                                                                  │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ UserController (Presentation Layer)                        │  │
│  │ • Handles HTTP requests                                    │  │
│  │ • Validates input (@Valid)                                 │  │
│  │ • Returns ApiResponse wrapper                              │  │
│  └───────────────────┬───────────────────────────────────────┘  │
│                      │ Delegates to                             │
│                      ▼                                           │
│  ┌───────────────────────────────────────────────────────────┐  │
│  │ UserService (Service Layer)                                │  │
│  │ ───────────────────────────────────────────────────────── │  │
│  │ Orchestrates:                                              │  │
│  │   1. Factory (create/update)                               │  │
│  │   2. Strategy (validate)                                   │  │
│  │   3. Repository (persist)                                  │  │
│  │   4. Mapper (entity ↔ DTO)                                 │  │
│  └─┬──────────┬──────────┬──────────┬─────────────────────────┘  │
│    │          │          │          │                            │
│    ▼          ▼          ▼          ▼                            │
│  ┌──────┐  ┌──────┐  ┌──────┐  ┌──────┐                         │
│  │ 🏭   │  │ 🎯   │  │ 🗄️   │  │ 📦   │                         │
│  └──────┘  └──────┘  └──────┘  └──────┘                         │
│  Factory   Strategy Repository Mapper                           │
└─────────────────────────────────────────────────────────────────┘

Flow:
1. Controller receives HTTP request
2. Service orchestrates patterns:
   a. Factory creates/updates entity
   b. Strategy validates based on account type
   c. Repository persists to database
   d. Mapper converts entity to DTO
3. Controller wraps in ApiResponse and returns
```

---

*Visual reference for understanding the complete system architecture, flow, and patterns.* 🎨

